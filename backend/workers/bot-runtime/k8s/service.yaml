apiVersion: v1
kind: Service
metadata:
  name: ultra-bot-runtime
  labels:
    app: ultra-bot-runtime
spec:
  selector:
    app: ultra-bot-runtime
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
