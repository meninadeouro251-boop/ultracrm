apiVersion: apps/v1
kind: Deployment
metadata:
  name: ultra-bot-runtime
  labels:
    app: ultra-bot-runtime
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ultra-bot-runtime
  template:
    metadata:
      labels:
        app: ultra-bot-runtime
    spec:
      containers:
        - name: ultra-bot-runtime
          image: ultra-bot-runtime:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "128Mi"
          env:
            # Non-secret vars from ConfigMap
            - name: LISTEN_ADDR
              valueFrom:
                configMapKeyRef:
                  name: ultra-bot-runtime-config
                  key: LISTEN_ADDR
            - name: AI_PROCESSOR_URL
              valueFrom:
                configMapKeyRef:
                  name: ultra-bot-runtime-config
                  key: AI_PROCESSOR_URL
            - name: AI_CALL_TIMEOUT_SECONDS
              valueFrom:
                configMapKeyRef:
                  name: ultra-bot-runtime-config
                  key: AI_CALL_TIMEOUT_SECONDS
            # Secrets
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: ultra-bot-runtime-secret
                  key: REDIS_URL
            - name: BOT_RUNTIME_SECRET
              valueFrom:
                secretKeyRef:
                  name: ultra-bot-runtime-secret
                  key: BOT_RUNTIME_SECRET
            - name: AI_PROCESSOR_API_KEY
              valueFrom:
                secretKeyRef:
                  name: ultra-bot-runtime-secret
                  key: AI_PROCESSOR_API_KEY
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
            failureThreshold: 2
